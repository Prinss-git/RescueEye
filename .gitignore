# Dependencies
node_modules/
__pycache__/
*.pyc
.venv/
venv/

# Environment — never commit real env files
.env
.env.local
*.env.local
frontend/.env.production
firebase-service-account.json

# Build outputs
dist/
build/
.vite/

# Misc
.DS_Store
Thumbs.db
*.log

# Models (large binaries — use Git LFS or share separately)
*.pt
*.onnx
api/models/

# Video demo feeds (large files — share separately)
api/data/*.mp4
api/data/*.avi
api/data/*.mkv

# Training datasets (images/labels — retrain separately)
api/data/damage/train/
api/data/damage/val/
api/data/damage/test/
api/data/victim/images/
api/data/victim/labels/
api/data/raw/
tools/datasets/

# YOLO base weights in root
api/yolov8n.pt
api/yolov8n-cls.pt

# Inference / run logs
api/logs/
*.jsonl
